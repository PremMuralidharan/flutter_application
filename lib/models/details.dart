class Details {
  int id;
  String title;
  String description;
  String rollno;
  String gender;
  String mobileno;
  String createdAt;
  Details( this.id, this.title, this.description, this.rollno, this.gender, this.mobileno, this.createdAt);
}